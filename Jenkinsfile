pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python3 --version'
                sh 'echo "hello world"'
                sh 'cat result.txt'
            }
        }
    }
}